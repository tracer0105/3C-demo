<Window x:Class="EthercatEsi.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:core="clr-namespace:EthercatEsi.Core;assembly=EthercatEsi.Core"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="EtherCAT ESI 伺服驱动解析器"
        Height="820"
        MinHeight="660"
        Width="1280"
        MinWidth="1080"
        Loaded="Window_Loaded">
    <Window.Resources>
        <HierarchicalDataTemplate DataType="{x:Type core:EsiNode}" ItemsSource="{Binding Children}">
            <TextBlock Text="{Binding DisplayName}" ToolTip="{Binding Meaning}" TextTrimming="CharacterEllipsis" />
        </HierarchicalDataTemplate>

        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="SelectionMode" Value="Single" />
            <Setter Property="Margin" Value="0,8,0,0" />
        </Style>
    </Window.Resources>

    <DockPanel Background="#F5F7FA">
        <Border DockPanel.Dock="Top" Background="#26323F" Padding="14,10">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                    <Button Content="打开 XML..." Click="OpenFiles_Click" MinWidth="104" Height="30" Margin="0,0,8,0" />
                    <Button Content="加载示例" Click="LoadSamples_Click" MinWidth="92" Height="30" />
                </StackPanel>
                <TextBlock Text="{Binding StatusText}" Foreground="#F7FAFC" Margin="16,0,0,0" TextTrimming="CharacterEllipsis" />
            </DockPanel>
        </Border>

        <Grid Margin="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360" MinWidth="300" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="220" />
                    <RowDefinition Height="10" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <GroupBox Header="XML 文件">
                    <ListBox ItemsSource="{Binding Documents}"
                             SelectedItem="{Binding SelectedDocument, Mode=TwoWay}"
                             DisplayMemberPath="DisplayName"
                             BorderThickness="0"
                             Margin="6" />
                </GroupBox>

                <GroupBox Grid.Row="2" Header="节点树">
                    <TreeView ItemsSource="{Binding SelectedTreeRoots}"
                              SelectedItemChanged="NodeTree_SelectedItemChanged"
                              BorderThickness="0"
                              Margin="6"
                              VirtualizingStackPanel.IsVirtualizing="True"
                              VirtualizingStackPanel.VirtualizationMode="Recycling" />
                </GroupBox>
            </Grid>

            <GridSplitter Grid.Column="1"
                          Width="10"
                          HorizontalAlignment="Stretch"
                          Background="#D8DEE7" />

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <DockPanel Margin="0,0,0,10">
                    <TextBlock DockPanel.Dock="Left" Text="当前设备" FontWeight="SemiBold" Margin="0,0,8,0" />
                    <ComboBox ItemsSource="{Binding SelectedDocument.Devices}"
                              SelectedItem="{Binding SelectedDevice, Mode=TwoWay}"
                              DisplayMemberPath="DisplayName"
                              MinWidth="520"
                              MaxWidth="760"
                              Height="28" />
                </DockPanel>

                <TabControl Grid.Row="1">
                    <TabItem Header="摘要">
                        <Grid Margin="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <TextBox Text="{Binding DocumentSummary,Mode=TwoWay,UpdateSourceTrigger=Default}"
                                     IsReadOnly="True"
                                     TextWrapping="Wrap"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     FontSize="14"
                                     Margin="0,0,0,10" />

                            <DataGrid Grid.Row="1"
                                      ItemsSource="{Binding SelectedDocument.Devices}"
                                      SelectedItem="{Binding SelectedDevice, Mode=TwoWay}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="设备名称" Binding="{Binding Name}" Width="2*" />
                                    <DataGridTextColumn Header="Type" Binding="{Binding TypeName}" Width="1.4*" />
                                    <DataGridTextColumn Header="ProductCode" Binding="{Binding ProductCode}" Width="120" />
                                    <DataGridTextColumn Header="RevisionNo" Binding="{Binding RevisionNumber}" Width="120" />
                                    <DataGridTextColumn Header="Profile" Binding="{Binding ProfileNumber}" Width="80" />
                                    <DataGridTextColumn Header="Objects" Binding="{Binding Objects.Count}" Width="80" />
                                    <DataGridTextColumn Header="RxPDO" Binding="{Binding RxPdos.Count}" Width="70" />
                                    <DataGridTextColumn Header="TxPDO" Binding="{Binding TxPdos.Count}" Width="70" />
                                    <DataGridTextColumn Header="SM" Binding="{Binding SyncManagers.Count}" Width="60" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </TabItem>

                    <TabItem Header="对象字典">
                        <DataGrid ItemsSource="{Binding SelectedDevice.Objects}" Margin="10">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Index" Binding="{Binding Index}" Width="90" />
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="1.4*" />
                                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100" />
                                <DataGridTextColumn Header="Bit" Binding="{Binding BitSize}" Width="60" />
                                <DataGridTextColumn Header="Access" Binding="{Binding Access}" Width="70" />
                                <DataGridTextColumn Header="Default" Binding="{Binding DefaultValue}" Width="130" />
                                <DataGridTextColumn Header="含义" Binding="{Binding Meaning}" Width="2.2*" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>

                    <TabItem Header="PDO 映射">
                        <DataGrid ItemsSource="{Binding SelectedDevice.PdoEntries}" Margin="10">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="方向" Binding="{Binding Direction}" Width="70" />
                                <DataGridTextColumn Header="PDO" Binding="{Binding PdoIndex}" Width="90" />
                                <DataGridTextColumn Header="PDO 名称" Binding="{Binding PdoName}" Width="1.2*" />
                                <DataGridTextColumn Header="SM" Binding="{Binding SyncManager}" Width="55" />
                                <DataGridTextColumn Header="#" Binding="{Binding EntryNumber}" Width="45" />
                                <DataGridTextColumn Header="Index" Binding="{Binding Index}" Width="90" />
                                <DataGridTextColumn Header="Sub" Binding="{Binding SubIndex}" Width="65" />
                                <DataGridTextColumn Header="Bit" Binding="{Binding BitLength}" Width="60" />
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="1.1*" />
                                <DataGridTextColumn Header="Type" Binding="{Binding DataType}" Width="90" />
                                <DataGridTextColumn Header="含义" Binding="{Binding Meaning}" Width="2*" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>

                    <TabItem Header="SM / FMMU">
                        <Grid Margin="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="2*" />
                                <RowDefinition Height="12" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <DataGrid ItemsSource="{Binding SelectedDevice.SyncManagers}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="SM" Binding="{Binding Number}" Width="55" />
                                    <DataGridTextColumn Header="用途" Binding="{Binding Usage}" Width="100" />
                                    <DataGridTextColumn Header="Start" Binding="{Binding StartAddress}" Width="100" />
                                    <DataGridTextColumn Header="Default" Binding="{Binding DefaultSize}" Width="80" />
                                    <DataGridTextColumn Header="Min" Binding="{Binding MinSize}" Width="70" />
                                    <DataGridTextColumn Header="Max" Binding="{Binding MaxSize}" Width="70" />
                                    <DataGridTextColumn Header="Control" Binding="{Binding ControlByte}" Width="80" />
                                    <DataGridTextColumn Header="Enable" Binding="{Binding Enable}" Width="70" />
                                    <DataGridTextColumn Header="含义" Binding="{Binding Meaning}" Width="2*" />
                                </DataGrid.Columns>
                            </DataGrid>

                            <GroupBox Grid.Row="2" Header="FMMU">
                                <ListBox ItemsSource="{Binding SelectedDevice.Fmmus}" BorderThickness="0" Margin="6" />
                            </GroupBox>
                        </Grid>
                    </TabItem>

                    <TabItem Header="当前节点说明">
                        <Grid Margin="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="110" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="节点" FontWeight="SemiBold" />
                            <TextBlock Grid.Column="1" Text="{Binding SelectedNode.DisplayName}" TextWrapping="Wrap" />

                            <TextBlock Grid.Row="1" Text="元素名" FontWeight="SemiBold" Margin="0,8,0,0" />
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedNode.ElementName}" Margin="0,8,0,0" />

                            <TextBlock Grid.Row="2" Text="值/属性" FontWeight="SemiBold" Margin="0,8,0,0" />
                            <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,8,0,0">
                                <TextBlock Text="{Binding SelectedNode.Value}" TextWrapping="Wrap" />
                                <TextBox Text="{Binding SelectedNode.AttributesText}"
                                         IsReadOnly="True"
                                         BorderThickness="0"
                                         Background="Transparent"
                                         TextWrapping="Wrap" />
                            </StackPanel>

                            <TextBlock Grid.Row="3" Text="含义" FontWeight="SemiBold" Margin="0,12,0,0" VerticalAlignment="Top" />
                            <TextBox Grid.Row="3"
                                     Grid.Column="1"
                                     Text="{Binding SelectedNode.Meaning}"
                                     IsReadOnly="True"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Auto"
                                     BorderThickness="1"
                                     Padding="8"
                                     Margin="0,12,0,0" />
                        </Grid>
                    </TabItem>
                </TabControl>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
